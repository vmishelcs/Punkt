#include <iostream>

#include <scanner/scanner.h>

int main(int argc, char **argv) {
	if (argc != 2) {
		std::cerr << "Token printer application requires an input file argument" << std::endl;
		return 0;
	}
}
