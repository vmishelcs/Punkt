function gcd <int a, int b> -> int {
	for , a != b, {
		if a > b {
			a = a - b.
		} else {
			b = b - a.
		}
	}
	return a.
}

main {
	println gcd(17, 13).
	println gcd(5, 25).
	println gcd(1071, 462).
}
