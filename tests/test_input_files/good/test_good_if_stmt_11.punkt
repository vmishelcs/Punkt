function divides <int x> -> void {
	if x % 3 == 0 {
		println "3 | ", x.
	} else if x % 4 == 0 {
		println "4 | ", x.
	} else if x % 6 == 0 {
		println "6 | ", x.
	} else {
		println "...".
	}
}

main {
	divides(2).
	divides(4).
	divides(3).
	divides(9).
	divides(8).
	divides(6).
	divides(11).
}
