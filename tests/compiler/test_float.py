from tests.compiler.punkt_compiler_test_helper import PunktCompilerTestHelper

class TestFloat(object):
  """ General floating point test suite. """

  def setup_class(self):
    self._compile_helper = PunktCompilerTestHelper()

  def test_good_float_1(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_2(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_3(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_4(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_5(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_6(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_7(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_8(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_9(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_10(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_11(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_12(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_13(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_14(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_15(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_16(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_17(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_18(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_19(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_20(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_21(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_22(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_23(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_24(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_25(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_26(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_27(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_28(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_29(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_30(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_31(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_32(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_33(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_34(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_35(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_36(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_37(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_38(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_39(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_40(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_41(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_42(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_43(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_44(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_45(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_46(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_47(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_48(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_49(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)

  def test_good_float_50(self):
    try:
      actual_output_path = self._compile_helper.compile_with_success()
      self._compile_helper.verify_actual_output(actual_output_path)
    except RuntimeError as ex:
      assert False, "Exception was raised: " + repr(ex)
